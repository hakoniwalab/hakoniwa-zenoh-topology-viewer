# Recipe configuration

Generated or template configuration for the `zenoh-topology-viewer` Recipe belongs here.

Expected configuration includes:

- PDU definition/config for `std_msgs/String`
- Endpoint configuration
- Bridge/WebSocket transfer configuration
- Viewer runtime configuration
