version: 1

build:
  type: Release
  dir: collector/build

components:
  collector: true
  web: true

validation:
  tests: true
  smoke: false

# Paths are resolved by the caller / Business Pack workspace.
paths:
  zenohc_root: ""
  pdu_endpoint_root: ""
  pdu_registry_root: ""
  pdu_javascript_root: ""
